package polymorphism.exercise.animals;

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        /*Scanner sc = new Scanner(System.in);
        String animalLine = sc.nextLine();
        while(!animalLine.equals("End")){
            String foodLine = sc.nextLine();
            String[] animalData = animalLine.split(" ");
            String[] foodData = foodLine.split(" ");
            String animalType = animalData[0];
            String animalName = animalData[1];
            double animalWeight = Double.parseDouble(animalData[2]);
            String livingRegion = animalData[3];

            String foodType = foodData[0];
            int foodEaten = Integer.parseInt(foodData[1]);
            Food food = null;
            if(foodType.equals("Vegetable")){
                food = new Vegetable(foodEaten);
            } else{
                food = new Meat(foodEaten);
            }
            switch(animalType){
                case "Cat":
                    Feline cat = new Cat(animalType, animalName, animalWeight, animalData[4]);
                    System.out.println(cat);
                    break;           }

            animalLine = sc.nextLine();
        }
    }*/
    }}
